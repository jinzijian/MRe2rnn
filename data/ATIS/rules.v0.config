[abbreviation]
( $ * ( mean | ( stand | stands ) for | code ) $ * )
( $ * what is $ EOS )
[aircraft]
$ * ( aircraft | airplane | plane | planes | airplanes ) $ *
[aircraft+flight+flight_no]
$ * ( aircraft | airplane | plane | planes | airplanes ) $ * flight number $ *
[airfare]
$ * ( airfare | fares | fare | cost | costs | class ( ticket | ticket ) | how much | ( price | prices ) of  ) $ * ( ( from $ * to $ * ) | ( between $ * and $ * ) ) $ *
[airfare+flight]
$ * airfare $* flight $ *
[airfare+flight_time]
$ * ( airfare | airfares | cost | how much ) $ * flight time $ *
[airline]
$ * ( airlines | airline ) $ *
[airline+flight_no]
$ * ( airlines | airline ) $ * flight ( number | numbers) $ *
[airport]
$ * ( airport | airports ) $ *
[capacity]
$ * ( ( capacities | capacity ) | how many ( seats | passengers | people ) | number of ( seats | passengers | people ) ) $ *
[cheapest]
$ * cheapest fare in the database $ *
[city]
$ * ( where is | cities | city ) $ *
[day_name]
$ * what day $ *
[distance]
$ * ( how far | how long | distance ) $ *
[flight]
$ * ( flights | flight | ( ( go | get | fly ) from $ * to $ * ) ) $ *
[flight+airfare]
$ * ( flights | flight ) and ( fare | fares ) $ *
[flight+airline]
$ * ( flight and airlines ) $ *
[flight_no]
$ * ( ( ( flight | flights ) number ) | ( number of ( flight | flights ) ) ) $ *
[flight_no+airline]
$ * flight ( number | numbers) $ * ( airlines | airline ) $ *
[flight_time]
$ * ( schedule | schedules | ( ( what | departure | arrival | flight | flights ) $ * ( time | times ) ) ) $ *
[ground_fare]
$ * ( price | prices | cost | costs ) $ *  ( ( ( rent | rental ) $ * ( car | cars ) ) | ( ( limousine | limo ) service ) ) $ *
[ground_service]
$ * ( ground ( transport | transportation ) | ( ( rent | rental ) $ * ( car | cars ) ) | ( ( limousine | limo ) service ) ) $ *
[ground_service+ground_fare]
$ * ( ground ( transport | transportation ) ) $ * how much $ *
[meal]
$ * ( meal | meals ) $ *
[quantity]
$ * ( how many  $ * ( flights | flight | code | stops | airports | booking class | fares | fare ) ) $ *
[restriction]
$ * ( what $ * ( restriction | restrictions ) ) $ *